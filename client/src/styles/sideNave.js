import { makeStyles } from "@material-ui/core/styles";

export default makeStyles(() => ({
  inputField: {
    display: "flex",
    padding: "15px",
    backgroundColor: "white",
  },

  drumInstruments: {
    
  }
}));
